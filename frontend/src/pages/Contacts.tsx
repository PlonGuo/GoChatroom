import { Typography } from 'antd';

const { Title } = Typography;

export const Contacts = () => {
  return (
    <div style={{ padding: 24 }}>
      <Title level={2}>Contacts</Title>
      <p>Contact list will be implemented here</p>
    </div>
  );
};

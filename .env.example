# Application
APP_ENV=development
PORT=8080

# Database (MySQL) - Use 127.0.0.1 (not localhost) to force TCP connection to Docker
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=gochatroom
DB_PASSWORD=gochatroom
DB_NAME=gochatroom

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Frontend URLs (for CORS)
CORS_ORIGINS=http://localhost:5173

# JWT Secret
JWT_SECRET=your-secret-key-change-in-production

# WebRTC TURN Server (optional for local dev)
TURN_SERVER_URL=
TURN_USERNAME=
TURN_PASSWORD=
